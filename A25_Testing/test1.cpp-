#define CATCH_CONFIG_MAIN  // This tells Catch to provide a main() - only do this in one cpp file
#include "catch.h"
#include "card.h"
#include "hand.h"
using namespace std;

bool isRevoke() {
    return true;
}



SCENARIO( "cards" ) {
    GIVEN("hand of cards"){}
    GIVEN("hand of cards"){
        Hand hand = {7, 21, 49, 50, 51, 24, 26, 27, 28, 33, 34, 35, 36};
        WHEN("hand dealt") {
        }
        THEN("print out is") {
            REQUIRE( hand[0].id() == "9, c, false");
            REQUIRE( hand[1].id() == "T, d, false");
            REQUIRE( hand[2].id() == "Q, s, false");
            REQUIRE( hand[3].id() == "K, s, false");
            REQUIRE( hand[4].id() == "A, s, false");
            REQUIRE( hand[5].id() == "K, d, false");
            REQUIRE( hand[6].id() == "2, h, false");
            REQUIRE( hand[7].id() == "3, h, false");
            REQUIRE( hand[8].id() == "4, h, false");
            REQUIRE( hand[9].id() == "9, h, false");
            REQUIRE( hand[10].id() == "T, h, false");
            REQUIRE( hand[11].id() == "J, h, false");
            REQUIRE( hand[12].id() == "Q, h, false");
        }
    }
}
